/**
 * Embedded catalog stub — no duplicate god/background JSON here.
 * Same-origin JSON files (godCharacter.json, …) are the source of truth; after each load
 * the merged catalogs are written to localStorage. Use “Remove localstorage” then reload
 * to pull fresh JSON from the server (e.g. GitHub Pages).
 */
window.MythologyMobileEmbeddedCatalog = {};
